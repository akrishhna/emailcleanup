About emailcleanup

Scans file uploaded by user to extract emailids.

Does the following with extracted emailids.
1. checks for the valid format.
2. checks for the valid domain.
3. verifies user and mailbox really exists.
4. validates emailids with out sending confirmation link to the user.
5. displays a table showing all emails, valid emails and invalid emails.
6. user can export emails to csv or xls file.

Application is working fine with the file having list of gmail ids. 

Working on it, to make this app work for all ids for all domain names. 